<UserControl x:Class="CollegeIdManagement.Views.Users.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Border Style="{StaticResource SectionCard}">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="System Administrative Users" FontSize="16" FontWeight="Bold" Foreground="#751A38" VerticalAlignment="Center" />
                    <Button Content="+ Add User" Command="{Binding AddUserCommand}" Style="{StaticResource ModernButton}" Margin="20,0,0,0" Padding="12,4" />
                </StackPanel>

                <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120" />
                        <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*" />
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180" />
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100" />
                        <DataGridTextColumn Header="Status" Binding="{Binding IsActive}" Width="80" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
